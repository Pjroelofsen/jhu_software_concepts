# requirements.txt for Pizza Application

# Runtime dependencies
# (None—this package uses only the Python standard library)

# Documentation dependencies
sphinx>=7.0.0,<8.0.0
sphinx-autobuild>=2023.14.1,<2024.0.0
sphinx-rtd-theme>=1.3.0,<2.0.0

# Testing dependencies (optional, if you add tests)
pytest>=7.0.0,<8.0.0
